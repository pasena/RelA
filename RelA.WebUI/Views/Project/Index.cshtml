@model RelA.WebUI.Models.ProjectViewModel
@using RelA.Domain.Entities;

@{
    ViewBag.Title = "Consultar Usuário";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>
    Pesquisar Usuários</h2>
<hr />
@using (Html.BeginForm())
{
    <span class="label">Projeto:</span>
    <br />
    @Html.TextBoxFor(m => m.Filter.Name)
    <br />
    <input type="submit" value="Pesquisar" class="actionButtons" />
}
<h3>
    Resultado</h3>
<div class="toolBar">@Html.ActionLink("Create a new Project", "Create")</div>
<hr />
@if (Model.Entity != null && Model.Entity.Count() > 0)
{
    <table class="Grid">
        <thead>
            <tr>
                <th>
                    Projeto
                </th>
                <th>
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (Project item in Model.Entity)
            {
                <tr>
                    <td>
                        @Html.ActionLink(item.Name, "Edit", new { projectID = item.ProjectID })
                    </td>
                    <td class="buttons">
                        @using (Html.BeginForm("Delete", "Project"))
                        {
                            @Html.Hidden("projectID", item.ProjectID)
                            <input type="submit" value="Delete" class="actionButtons" onclick="return confirm('You really want to delete this user?');" />
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>    
    <div class="pager">
        @Html.PageLinks(Model.PagingInfo, x => Url.Action("Index", new { page = x }))
    </div>
}
else
{
    @:Nenhum registro encontrado!
}